stages:
  - build
  - package
  - deploy

variables:
  SERVICE_NAME: iadp-admin-web

build:
  image: $REGISTRY_ADDRESS/devops/nodejs/build
  stage: build
  only:
    - develop
    - testing
    - release
  script:
    - yarn && yarn lint && yarn build
  artifacts:
    paths:
      - dist
      - Dockerfile
    expire_in: 2 hrs
  tags:
    - nodejs

package:
  image: $REGISTRY_ADDRESS/devops/docker/package
  stage: package
  variables:
    GIT_STRATEGY: none
  only:
    - develop
    - testing
    - release
  script:
    - package
  tags:
    - docker

deploy:
  image: $REGISTRY_ADDRESS/devops/docker/deploy
  stage: deploy
  variables:
    GIT_STRATEGY: none
  dependencies: []
  only:
    - develop
    - testing
    - release
  script:
    - deploy -service $SERVICE_NAME
  environment: aiads-dev
  tags:
    - docker
