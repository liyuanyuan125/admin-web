/**
 * 该文件是环境配置文件，-dev 表示开发环境，-prod 为生成环境，不要直接使用，通过
 * import { ... } from 'env' 使用
 * 其中 env 是 webpack 的别名，根据不同的环境，自动切换 env-dev 还是 env-prod
 */

export const ajaxBaseUrl = ''

export const debug = true
